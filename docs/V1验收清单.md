# V1 验收清单

> 鑫连鑫丝网厂 B2B 官网 V1 部署前验收
> 创建日期：2026-06-05
> V1.2 界面美化已完成，详见 [V1.2验证清单.md](V1.2验证清单.md)

---

## 一、前台展示（xlx-web）

### 1.1 首页

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| Hero Banner | 访问 `/` | 显示"鑫连鑫丝网厂"标题、23年生产经验副标题、两个 CTA 按钮 |
| 卖点区块 | 查看 Hero 下方 | 显示 4 个卖点：自有工厂、品质保障、定制加工、出口经验 |
| 产品分类 | 查看产品分类区 | 显示数据库中的产品分类，可点击跳转到 `/products?category={id}` |
| 热门产品 | 查看热门产品区 | 最多显示 8 个产品卡片，含图片、名称、分类 |
| 工厂实景 | 查看工厂区 | 显示 3 张工厂图片（或占位），"查看更多"链接到 `/factory` |
| 资质证书 | 查看证书区 | 显示数据库中的证书（最多 4 张），可点击查看全部 |
| 常见问题 | 查看 FAQ 区 | 显示 5 个常见问题，点击展开/收起答案 |
| 联系 CTA | 查看底部 | "在线询盘"和"电话咨询"按钮 |

### 1.2 产品列表页

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 分类筛选 | 访问 `/products`，点击左侧分类 | 可按分类筛选产品 |
| 产品列表 | 查看产品网格 | 显示产品图片、名称、分类 |
| 分页 | 点击分页按钮 | 可翻页浏览 |
| 搜索 | 输入关键词 | 可按名称搜索产品 |

### 1.3 产品详情页

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 产品信息 | 访问 `/products/{slug}` | 显示产品名称、描述、规格表 |
| 图片展示 | 查看产品图片区 | 可查看主图和详情图 |
| 询盘按钮 | 点击"询盘"按钮 | 弹出询盘表单 |
| 表单提交 | 填写表单并提交 | 提交成功显示感谢提示，失败显示错误信息 |

### 1.4 工厂展示页

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 工厂图片 | 访问 `/factory` | 显示工厂图片列表 |
| 图片详情 | 点击图片 | 可查看大图 |

### 1.5 证书展示页

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 证书列表 | 访问 `/certificates` | 显示所有证书，含名称和图片 |

### 1.6 FAQ 页

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 问题列表 | 访问 `/faq` | 显示所有常见问题 |
| 展开收起 | 点击问题 | 可展开/收起答案 |

### 1.7 联系我们页

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 联系方式 | 访问 `/contact` | 显示电话、邮箱、微信、WhatsApp、地址 |
| 询盘表单 | 填写表单并提交 | 提交成功显示感谢提示 |
| 文件上传 | 选择附件后提交 | 附件上传成功，表单提交成功 |

### 1.8 页脚

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 公司信息 | 查看页脚 | 显示公司名称、简介 |
| 快速链接 | 查看页脚 | 显示 5 个页面链接 |
| 联系方式 | 查看页脚 | 显示电话、邮箱、微信、WhatsApp、地址 |
| 版权信息 | 查看页脚底部 | 显示当前年份 + "鑫连鑫丝网厂 All Rights Reserved" |

---

## 二、后台管理（xlx-admin）

### 2.1 登录

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 登录页 | 访问 `/admin/login` | 显示用户名、密码输入框和登录按钮 |
| 正确登录 | 输入 admin/admin123，点击登录 | 登录成功，跳转到 Dashboard |
| 错误登录 | 输入错误密码 | 显示错误提示 |

### 2.2 Dashboard

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 统计卡片 | 查看 Dashboard | 显示产品总数、询盘总数、证书数、文件数 |
| 今日询盘 | 查看今日询盘列表 | 显示今日新增的询盘 |
| 今日新增产品 | 查看今日产品列表 | 显示今日新增的产品 |

### 2.3 产品管理

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 产品列表 | 点击"产品管理" | 显示分页产品列表 |
| 新增产品 | 点击"新增"，填写表单，提交 | 新增成功，列表刷新 |
| 编辑产品 | 点击"编辑"，修改信息，提交 | 修改成功，列表刷新 |
| 删除产品 | 点击"删除"，二次确认 | 删除成功，列表刷新 |
| 上下架 | 切换状态开关 | 状态切换成功 |

### 2.4 分类管理

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 分类列表 | 点击"分类管理" | 显示所有分类 |
| 新增分类 | 填写名称，提交 | 新增成功 |
| 编辑分类 | 修改名称，提交 | 修改成功 |
| 删除分类 | 点击"删除"，二次确认 | 删除成功（需先删除该分类下产品） |

### 2.5 询盘管理

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 询盘列表 | 点击"询盘管理" | 显示分页询盘列表 |
| 询盘详情 | 点击"查看" | 显示完整询盘信息 |
| 修改状态 | 选择新状态，提交 | 状态更新成功 |
| 修改备注 | 输入备注，提交 | 备注更新成功 |
| 删除询盘 | 点击"删除"，二次确认 | 删除成功 |

### 2.6 证书管理

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 证书列表 | 点击"证书管理" | 显示所有证书 |
| 新增证书 | 填写名称，上传图片，提交 | 新增成功 |
| 编辑证书 | 修改信息，提交 | 修改成功 |
| 删除证书 | 点击"删除"，二次确认 | 删除成功 |

### 2.7 工厂图片管理

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 图片列表 | 点击"工厂图片" | 显示所有工厂图片 |
| 上传图片 | 选择图片，提交 | 上传成功 |
| 删除图片 | 点击"删除"，二次确认 | 删除成功 |

### 2.8 网站配置

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 配置表单 | 点击"网站配置" | 显示所有配置项 |
| 修改配置 | 修改信息，提交 | 保存成功，前台生效 |

### 2.9 移动端适配

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 侧边栏折叠 | 在 <768px 宽度下访问 | 侧边栏自动折叠，显示汉堡菜单 |
| 汉堡菜单 | 点击汉堡菜单 | 展开/收起侧边栏 |
| 统计卡片 | 在 320px 宽度下查看 | 每行显示 2 个卡片 |
| 表格滚动 | 在移动端查看表格 | 可横向滚动 |

---

## 三、接口验收（xlx-api）

### 3.1 公开接口

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 获取分类 | `GET /api/public/categories` | 返回分类列表 |
| 获取产品 | `GET /api/public/products?page=1&size=10` | 返回分页产品列表 |
| 产品详情 | `GET /api/public/products/{slug}` | 返回产品详情 |
| 获取证书 | `GET /api/public/certificates` | 返回证书列表 |
| 获取配置 | `GET /api/public/site-config` | 返回网站配置 |
| 提交询盘 | `POST /api/public/inquiries` | 提交成功返回 200 |
| 文件上传 | `POST /api/public/upload` | 返回文件 URL |

### 3.2 管理接口

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 登录 | `POST /api/admin/auth/login` | 返回 JWT token |
| 获取用户信息 | `GET /api/admin/auth/profile`（带 token） | 返回用户信息 |
| 产品 CRUD | `GET/POST/PUT/DELETE /api/admin/products` | 各操作正常 |
| 分类 CRUD | `GET/POST/PUT/DELETE /api/admin/categories` | 各操作正常 |
| 询盘管理 | `GET/PUT/DELETE /api/admin/inquiries` | 各操作正常 |
| 证书 CRUD | `GET/POST/PUT/DELETE /api/admin/certificates` | 各操作正常 |
| 工厂图片 CRUD | `GET/POST/DELETE /api/admin/factory-images` | 各操作正常 |
| 网站配置 | `GET/PUT /api/admin/site-config` | 各操作正常 |
| 文件管理 | `GET /api/admin/upload`、`DELETE /api/admin/upload/{id}` | 各操作正常 |

### 3.3 认证校验

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 无 token 访问管理接口 | `GET /api/admin/products`（不带 token） | 返回 401 |
| 过期 token | 使用过期 token 访问 | 返回 401 |
| 无效 token | 使用伪造 token 访问 | 返回 401 |
| 公开接口免认证 | `GET /api/public/products` | 正常返回数据 |

---

## 四、文件上传

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 图片上传 | 上传 JPG/PNG 图片 | 上传成功，返回可访问 URL |
| PDF 上传 | 上传 PDF 文件 | 上传成功 |
| 文件大小限制 | 上传 >10MB 文件 | 返回错误提示 |
| 格式限制 | 上传 .exe 文件 | 返回格式不支持错误 |
| 目录结构 | 上传文件后检查 uploads 目录 | 按 `bizType/yyyy/MM/` 组织 |
| 静态访问 | 通过 URL 访问上传的文件 | 可正常访问 |
| 文件列表 | `GET /api/admin/upload` | 返回分页文件列表 |
| 文件删除 | `DELETE /api/admin/upload/{id}` | 记录和物理文件均删除 |

---

## 五、移动端适配

### 5.1 前台移动端

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 首页响应式 | 320px/375px 宽度访问首页 | 所有区块正常显示，无溢出 |
| 产品列表 | 320px 宽度查看产品页 | 产品网格自适应，2 列/1 列 |
| 产品详情 | 320px 宽度查看详情 | 规格表可横向滚动 |
| 询盘表单 | 320px 宽度提交询盘 | 表单正常显示和提交 |
| 触摸目标 | 检查所有按钮和链接 | 可点击区域 ≥ 44px |

### 5.2 后台移动端

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 侧边栏折叠 | 768px 以下访问后台 | 侧边栏自动折叠 |
| 汉堡菜单 | 点击汉堡图标 | 展开侧边栏 |
| 表单布局 | 移动端填写表单 | 表单字段单列排列 |
| 表格滚动 | 移动端查看列表 | 可横向滚动 |

---

## 六、安全检查

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| JWT 认证 | 管理接口不带 token | 返回 401 |
| SQL 注入 | 在表单输入 `' OR 1=1 --` | 被拦截，无异常 |
| XSS | 在表单输入 `<script>alert(1)</script>` | 被转义，不执行 |
| 文件类型限制 | 上传可执行文件 | 被拒绝 |
| 文件大小限制 | 上传超大文件 | 被拒绝 |
| CORS 配置 | 检查跨域配置 | 仅允许指定域名 |
| 密码存储 | 检查数据库中密码 | 使用 MD5 加密存储 |
| 错误信息 | 触发服务器错误 | 不泄露堆栈信息 |

---

## 七、部署准备

### 7.1 构建验证

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 后端测试 | `cd xlx-api && mvn clean test` | 22 个测试全部通过 |
| 后端构建 | `cd xlx-api && mvn clean package -DskipTests` | 构建成功，生成 JAR |
| 前台构建 | `cd xlx-web && npm run build` | 构建成功，生成 dist/ |
| 后台构建 | `cd xlx-admin && npm run build` | 构建成功，生成 dist/ |

### 7.2 配置检查

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| 数据库配置 | 检查 application.yml | 数据库连接信息正确 |
| 文件上传路径 | 检查 application.yml | upload-path 配置正确 |
| JWT 密钥 | 检查 application.yml | 支持 `JWT_SECRET` 环境变量覆盖 |
| CORS 配置 | 检查 application.yml | 支持 `CORS_ORIGINS` 环境变量覆盖，开发环境默认 localhost:3000/3001 |
| 前端 API 地址 | 检查 vite.config.ts | 代理配置指向 localhost:8080 |
| 后台 API 地址 | 检查 vite.config.ts | 代理配置指向 localhost:8080 |

### 7.3 部署清单

| 验收项 | 操作步骤 | 预期结果 |
|--------|----------|----------|
| MySQL | 创建数据库，执行 Flyway 迁移 | 表结构创建成功 |
| Java 环境 | 确认服务器 Java 17+ | 版本满足要求 |
| Nginx | 配置反向代理 | 前台/后台/API 路由正确 |
| uploads 目录 | 创建并设置权限 | 目录存在，可写 |
| 环境变量 | 设置生产环境变量 | DB_PASSWORD、JWT_SECRET、CORS_ORIGINS、UPLOAD_PATH |

### 7.4 本地开发

详见 [本地开发与联调说明.md](本地开发与联调说明.md)

---

## 八、已发现问题与修复记录

### 已修复

| 问题 | 级别 | 修复内容 | 修复日期 |
|------|------|----------|----------|
| MyBatis Plus logic-delete-field 配置未使用 | MEDIUM | 移除 application.yml 中的 `logic-delete-field` 配置及 SQL 注释 | 2026-06-05 |
| JWT 密钥硬编码 | LOW | 已支持 `${JWT_SECRET:...}` 环境变量覆盖（原有机制） | 已有 |
| CORS 允许所有来源 | LOW | 改为 `${CORS_ORIGINS:...}` 环境变量配置，开发环境默认 localhost:3000/3001 | 2026-06-05 |

### 待处理（生产环境）

| 问题 | 级别 | 说明 |
|------|------|------|
| JWT 密钥使用默认值 | LOW | 本地开发可用默认值，生产环境需设置 `JWT_SECRET` 环境变量 |
| CORS 需限制生产域名 | LOW | 本地开发可用 localhost，生产环境需设置 `CORS_ORIGINS` 为实际域名 |

---

## 九、建议下一步处理顺序

1. **【已完成】清理 logic-delete-field 配置**
2. **【已完成】CORS 改为环境变量配置**
3. **【验证】本地联调验证 — 按本地开发说明启动并测试
4. **【生产准备】设置生产环境变量（JWT_SECRET、CORS_ORIGINS、DB_PASSWORD）
5. **【部署】按部署清单准备生产环境

---

## 十、验收结论

- [x] 所有 CRITICAL 问题已修复（原报告为误报，application.yml 实际正确）
- [x] 所有 MEDIUM 问题已修复（logic-delete-field 已移除）
- [x] 构建验证通过（后端 22 个测试通过，前台/后台构建成功）
- [ ] 前台所有页面功能正常（待本地联调验证）
- [ ] 后台所有管理功能正常（待本地联调验证）
- [ ] 所有接口返回正确（待本地联调验证）
- [ ] 文件上传功能正常（待本地联调验证）
- [ ] 移动端适配正常（待本地联调验证）
- [ ] 安全检查通过（待本地联调验证）
- [ ] 部署清单准备完毕（待生产环境配置）

**验收人：** ________________

**验收日期：** ________________
