# V2.2 边界检查记录

**检查日期：** 2026-06-06

## 结论

**V2.2 代码边界检查通过，未发现越界内容。**

## 检查结果

| 检查项 | 结果 |
|--------|------|
| Buy Now / Checkout / Payment 文案 | 未发现 |
| 购物车 / Shopping Cart 命名 | 未发现 |
| 订单逻辑（Order） | 未发现（仅有询盘状态 `order_confirmed` 标签） |
| 支付逻辑（Pay） | 未发现（仅有 FAQ 静态文案"订单与付款"） |
| 支付 SDK（支付宝/微信/PayPal/Stripe） | 未发现 |
| 客户注册 / Register | 未发现 |
| 客户登录 / Login | 仅后台管理员登录（V1 已有） |
| 客户中心 / Account | 未发现 |
| 报价单 / Quotation | 未发现（仅有"联系报价"文案和"已报价"状态） |
| 自动报价 | 未发现 |
| CRM | 未发现 |
| 多角色权限 | 仅 admin/editor 两种基础角色（V1 已有） |
| localStorage 越界 key | 未发现（仅 `xlx_quote_cart`、`admin_token`、`xlx_lang`） |
| 后端购物车/订单 Entity | 未发现 |
| 后端购物车/订单 Controller | 未发现 |

## 数据库表清单（V1 + V9）

10 张业务表 + 1 张迁移新增表，无购物车/订单/支付表：

1. `admin_user`
2. `product_category`
3. `product`
4. `product_spec`
5. `product_image`
6. `factory_image`
7. `certificate`
8. `inquiry`
9. `file_upload`
10. `site_config`
11. `inquiry_item`（V2.2 新增）

## localStorage 清单

| Key | 所在文件 | 用途 |
|-----|---------|------|
| `xlx_quote_cart` | xlx-web useQuoteCart.ts | 询价车草稿 |
| `admin_token` | xlx-admin auth.ts | 管理后台 JWT |
| `xlx_lang` | xlx-web i18n.ts | 语言偏好 |

## 命名规范确认

- 项目中使用"询价车"（非"购物车"）
- 英文使用 "Quote Cart"（非 "Shopping Cart"）
- 按钮文案使用 "加入询价"、"提交询价"（非 "Buy Now"、"Checkout"）
