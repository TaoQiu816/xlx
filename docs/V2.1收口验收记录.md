# V2.1 收口验收记录

> 验收时间：2026-06-06
> 验收范围：V2.1 中英文切换功能完整性

## 验收结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| SiteConfig 英文配置项后台维护 | ✅ 通过 | 24 个英文字段全部暴露在 SiteConfig.vue，中英文成对排列 |
| FAQ 首页中英文 | ✅ 通过 | Home.vue 支持 qEn/aEn，使用 lang 切换 |
| FAQ 页面中英文 | ✅ 通过 | Faq.vue 支持 qEn/aEn/titleEn，使用 displayGroups 计算属性切换 |
| About 页面公司介绍 | ⚠️ 已知限制 | 公司介绍为组件内静态文本，未从 SiteConfig 读取（P2 待办） |
| 英文字段为空时回退中文 | ✅ 通过 | langField() 和手动 fallback 均实现了 `En \|\| 中文` 逻辑 |
| 中英文切换 | ✅ 通过 | vue-i18n + localStorage 持久化，切换即时生效 |
| 产品功能 | ✅ 通过 | 产品列表、详情、规格显示正常 |
| 询盘功能 | ✅ 通过 | 询盘提交、列表、详情正常 |
| 文件上传 | ✅ 通过 | 图片上传、证书上传正常 |
| 构建验证 | ✅ 通过 | xlx-admin、xlx-web、xlx-api 均构建成功 |
| 单元测试 | ✅ 通过 | 24 个测试全部通过（AuthService 4 + InquiryService 12 + FileUploadService 8） |

## 本轮修复内容

### P0: SiteConfig 英文后台入口
- **文件**: `xlx-admin/src/views/SiteConfig.vue`
- **改动**: 重写整个表单，为所有 24 个英文配置项添加成对的中英文输入框
- **覆盖**: 基本信息、联系方式、首页 Banner、Hero 统计数字、卖点区块、SEO 配置

### P1: FAQ JSON 中英文结构
- **文件**: `xlx-admin/src/views/SiteConfig.vue`
  - FAQ 编辑支持 qEn/aEn/titleEn 字段
  - 向后兼容旧格式 `{q, a}` 和新格式 `{q, a, qEn, aEn}`
- **文件**: `xlx-web/src/views/Faq.vue`
  - 添加 FaqItem 接口 qEn/aEn 字段
  - 添加 FaqGroup 接口 titleEn 字段
  - 新增 displayGroups 计算属性，根据 lang 切换中英文
  - 解析 config 时兼容旧格式和新格式
  - 添加完整的英文默认 FAQ 内容

## 已知限制（非阻塞）

1. **About.vue 公司介绍为静态文本** — 未从 SiteConfig 动态读取，P2 待办
2. **V7/V8 迁移文件列名不匹配** — `remark` vs `description`，已执行迁移不可修改，API 层已兼容
3. **Dashboard 未做 i18n** — 后台管理界面为中文，符合设计（后台仅管理员使用）

## 结论

V2.1 中英文切换功能已收口完成。核心功能（语言切换、双语内容、后台配置、前台展示）全部正常。剩余 P2/P3 级别待办不影响 V2.1 发布。
